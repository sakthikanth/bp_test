'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = createOrbitControls;

var _orbitControls = require('orbit-controls');

var _orbitControls2 = _interopRequireDefault(_orbitControls);

var _update = require('./update');

var _update2 = _interopRequireDefault(_update);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function OrbitControls(camera, properties) {
  var controls = (0, _orbitControls2.default)(properties);

  controls.camera = camera;
  controls.updateController = controls.update;
  controls.update = _update2.default.bind(null, controls);

  return controls;
}

function createOrbitControls(camera, properties) {
  return OrbitControls(camera, properties);
}