"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.getNodesChildren = getNodesChildren;
exports.default = getChildren;
function getNodesChildren(graph, node) {
  var children = graph.children.get(node);
  if (!children) {
    children = [];
    graph.children.set(node, children);
  }
  return children;
}

function getChildren(scene, graph, a) {
  var node = a || scene;

  return getNodesChildren(graph, node);
}